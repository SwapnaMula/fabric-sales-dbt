name: "fabric_sales"
version: "1.0.0"

profile: "fabric_sales"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fabric_sales:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold